import styles from "./Options.module.css";

const Options = () => {

    return(
        <div className={styles.container}>
            <p>Options</p>
        </div>
    )
}; 

export default Options;